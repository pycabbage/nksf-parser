# 015: 統合テストの拡充

## 概要

`lib/tests/integration.rs` に複数ファイル処理、エラーケース、パフォーマンステストを追加する。

## 完了条件

- [x] `lib/tests/integration.rs` が実装されている
- [x] 複数ファイルの連続解析テストが実装されている（test_parse_all_fixture_files）
  - 全720個のプリセットファイルを解析
  - 全て成功することを確認
- [x] エラーケースのテストが実装されている
  - test_invalid_riff_file
  - test_incomplete_file
  - test_nonexistent_file
  - test_invalid_niks_format
  - test_unknown_chunk
- [x] パフォーマンステストが実装されている（test_parse_performance, #[ignore]付き）
- [x] `cargo test -p nksf-parser` で全テストが成功すること
- [x] 構造体の柔軟性向上（modes, ni8フィールドをオプショナルに）

## 実装ガイド

### 複数ファイルの連続解析

```rust
#[test]
fn test_parse_multiple_files() {
    let fixture_dir = PathBuf::from(env!("CARGO_MANIFEST_DIR"))
        .join("tests")
        .join("massive_x_factory_library_tests")
        .join("fixture");

    let mut success_count = 0;
    let mut failure_count = 0;

    for entry in std::fs::read_dir(&fixture_dir).unwrap() {
        let entry = entry.unwrap();
        let path = entry.path();

        if path.extension().and_then(|s| s.to_str()) == Some("nksf") {
            match parse_nksf(&path) {
                Ok(_) => success_count += 1,
                Err(e) => {
                    eprintln!("Failed to parse {:?}: {}", path, e);
                    failure_count += 1;
                }
            }
        }
    }

    println!("Parsed {} files successfully, {} failures", success_count, failure_count);
    assert_eq!(failure_count, 0, "All files should parse successfully");
}
```

### エラーケースのテスト

```rust
#[test]
fn test_invalid_riff_file() {
    let invalid_data = b"INVALID DATA";
    let result = parse_nksf_from_bytes(invalid_data);
    assert!(matches!(result, Err(ParseError::InvalidRiff)));
}

#[test]
fn test_nonexistent_file() {
    let path = PathBuf::from("/nonexistent/file.nksf");
    let result = parse_nksf(&path);
    assert!(matches!(result, Err(ParseError::IoError(_))));
}

#[test]
fn test_incomplete_file() {
    // RIFFヘッダーのみのファイル
    let incomplete_data = b"RIFF\x10\x00\x00\x00NIKS";
    let result = parse_nksf_from_bytes(incomplete_data);
    assert!(result.is_err());
}
```

### パフォーマンステスト（オプション）

```rust
#[test]
#[ignore] // 通常のテスト実行では除外
fn test_parse_performance() {
    use std::time::Instant;

    let fixture_dir = PathBuf::from(env!("CARGO_MANIFEST_DIR"))
        .join("tests")
        .join("massive_x_factory_library_tests")
        .join("fixture");

    let mut total_time = std::time::Duration::ZERO;
    let mut file_count = 0;

    for entry in std::fs::read_dir(&fixture_dir).unwrap() {
        let entry = entry.unwrap();
        let path = entry.path();

        if path.extension().and_then(|s| s.to_str()) == Some("nksf") {
            let start = Instant::now();
            let _ = parse_nksf(&path).expect("Parse failed");
            total_time += start.elapsed();
            file_count += 1;
        }
    }

    let avg_time = total_time / file_count;
    println!("Average parse time: {:?}", avg_time);

    // 1ファイルあたり10ms以内の目標
    assert!(avg_time.as_millis() < 10, "Parse time too slow: {:?}", avg_time);
}
```

## 注意点

- パフォーマンステストは `#[ignore]` 属性を付けて通常のテストから除外
- エラーケースでは具体的なエラー型を確認
- 複数ファイルテストでは失敗したファイル名を出力
- テストデータは実際のfixtureディレクトリを使用

---

## 実装メモ

[実装時に発見した事柄や改善点などを記載]
