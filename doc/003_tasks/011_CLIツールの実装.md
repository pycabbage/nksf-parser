# 011: CLIツールの実装

## 概要

`cli/src/main.rs` を実装し、.nksfファイルをJSON形式で出力するCLIツールを作成する。

**前提条件**: タスク010（CLI依存関係の追加）が完了していること

## 完了条件

- [ ] `cli/src/main.rs` が実装されている
- [ ] `clap` を使用したコマンドライン引数処理が実装されている
- [ ] `nksf_parser::parse_nksf()` を呼び出して解析を実行している
- [ ] 解析結果をJSON形式で標準出力に出力している
- [ ] `--compact` フラグで整形なし/整形ありを切り替えられる
- [ ] エラーハンドリングが適切に実装されている（基本的なeprintln!とexit）
- [ ] `cargo build -p nksf-parser-cli` が成功すること
- [ ] 実際の.nksfファイルでCLIツールが動作することを確認
- [ ] `cargo run -p nksf-parser-cli -- <path>` でJSON出力ができること

## 実装ガイド

### main.rs の構成

```rust
use clap::Parser;
use nksf_parser;
use std::path::PathBuf;
use std::process;

/// .nksfファイルをJSON形式で出力するCLIツール
#[derive(Parser)]
#[command(name = "nksf-parser-cli")]
#[command(about = "Parse .nksf (Massive X preset) files and output as JSON")]
struct Args {
    /// Input .nksf file path
    input: PathBuf,

    /// Output compact JSON (no pretty printing)
    #[arg(short, long)]
    compact: bool,
}

fn main() {
    let args = Args::parse();

    // ファイルを解析
    let result = match nksf_parser::parse_nksf(&args.input) {
        Ok(nksf) => nksf,
        Err(e) => {
            eprintln!("Error parsing file: {}", e);
            process::exit(1);
        }
    };

    // JSON出力
    let json_output = if args.compact {
        match serde_json::to_string(&result) {
            Ok(json) => json,
            Err(e) => {
                eprintln!("Error serializing to JSON: {}", e);
                process::exit(1);
            }
        }
    } else {
        match serde_json::to_string_pretty(&result) {
            Ok(json) => json,
            Err(e) => {
                eprintln!("Error serializing to JSON: {}", e);
                process::exit(1);
            }
        }
    };

    println!("{}", json_output);
}
```

## 使用例

```bash
# 通常出力（整形あり）
cargo run -p nksf-parser-cli -- lib/tests/massive_x_factory_library_tests/fixture/Abandoned.nksf

# コンパクト出力
cargo run -p nksf-parser-cli -- lib/tests/massive_x_factory_library_tests/fixture/Abandoned.nksf --compact
```

## 注意点

- エラーメッセージは標準エラー出力（`eprintln!`）に出力
- JSON出力は標準出力（`println!`）に出力
- エラー時は適切な終了コード（1）を返す
- ユーザーフレンドリーなヘルプメッセージを提供

---

## 実装メモ

[実装時に発見した事柄や改善点などを記載]
