# 013: エラーハンドリングの改善（更新版）

## 概要

CLIツールでのユーザーフレンドリーなエラーメッセージを実装する。

**前提条件**: タスク011（CLIツールの実装）が完了していること

**注**: libクレートのエラーハンドリングは既に適切に実装済み（thiserror使用、適切なエラー伝播）。
このタスクは主にCLIのエラーメッセージ改善に焦点を当てる。

## 完了条件

### 既に完了（Phase 3で実装済み）:

- [x] libクレートで適切なエラー型が定義されている（error.rs）
- [x] エラー伝播が適切に実装されている（`?` 演算子の使用）
- [x] セキュリティエラーに対する理由説明がコメントで記載されている
- [x] 全エラーケースでテストが実装されている

### 未実装（このタスクで実装）:

- [x] CLIツールでユーザーフレンドリーなエラーメッセージが表示される
- [x] エラーの種類に応じた解決のヒントが表示される
- [x] エラー時に適切な終了コードを返す（exit(1)）
- [x] CLIの動作確認（存在しないファイル、正常なファイル）

## 実装内容

### CLIエラーメッセージの実装

`cli/src/main.rs` でエラーの種類に応じた分かりやすいメッセージを表示:

```rust
let result = match nksf_parser::parse_nksf(&args.input) {
    Ok(nksf) => nksf,
    Err(e) => {
        eprintln!("Error: Failed to parse '{}'", args.input.display());
        eprintln!("Reason: {}", e);

        // エラーの種類に応じた追加情報
        match e {
            ParseError::IoError(_) => {
                eprintln!("Hint: Check if the file exists and you have read permission.");
            }
            ParseError::InvalidRiff => {
                eprintln!("Hint: The file is not a valid RIFF file.");
            }
            // ... 他のエラーケース ...
            _ => {}
        }

        process::exit(1);
    }
};
```

## テストケース

CLIツールのエラーハンドリングに対するテストを追加:

- 存在しないファイルを指定した場合のエラーメッセージ
- 不正なフォーマットのファイルを指定した場合のエラーメッセージ
- JSON出力エラー時の処理

**注**: libクレートのエラーケーステストは既に実装済み（29個のユニットテスト）

## 注意点

- エラーメッセージは英語で記述
- ユーザーが問題を解決できるヒントを提供
- 開発者向けのデバッグ情報とユーザー向けのメッセージを区別
- エラーテストは `tests/` ディレクトリに配置

---

## 実装メモ

[実装時に発見した事柄や改善点などを記載]
