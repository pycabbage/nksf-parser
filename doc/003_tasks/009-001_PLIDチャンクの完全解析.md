# 009-001: PLIDチャンクの完全解析

## 概要

PLIDチャンク（Plugin ID）のMessagePackデータを完全に解析し、構造化する。

## 完了条件

- [x] PLIDチャンクの構造を調査（hexdumpとMessagePack解析）
- [x] 調査結果を `doc/001_research/003_plid_chunk_analysis.md` に記録
- [x] 複数プリセットで構造を検証（Abandoned, Alien Contact, All Rise）
- [x] 全73バイト（最大）の意味を特定

**✓ 調査完了。実装は 009-003 で実施。**

## 調査手順

### 1. PLIDチャンクのバイトデータを抽出

```bash
# Abandoned.nksfからPLIDチャンクを特定
hexdump -C lib/tests/massive_x_factory_library_tests/fixture/Abandoned.nksf | grep -A 10 "PLID"
```

### 2. MessagePackデータの解析

PLIDチャンクもバージョン情報（4バイト）+ MessagePackの構造と仮定して解析

### 3. 構造体の定義

```rust
/// PLIDチャンク（Plugin ID）
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PlidData {
    // 調査結果に基づいてフィールドを定義
    // 例:
    // pub vst_magic: Option<String>,
    // pub plugin_name: String,
    // pub plugin_vendor: String,
}
```

## 注意点

- PLIDチャンクは MessagePack でエンコードされている
- バージョン情報があるか確認
- 全フィールドを構造化して保持
- 「不明なデータ」として捨てることは許可されない

---

## 実装メモ

[実装時に発見した事柄や改善点などを記載]
