# 009-003: PLIDパーサーの実装

## 概要

調査結果に基づき、PLIDチャンクパーサーを実装し、メインパーサーに統合する。

## 完了条件

- [ ] `lib/src/types.rs` に `PlidData` 構造体を追加
- [ ] `lib/src/plid_parser.rs` を作成
- [ ] PLIDパーサーをメインパーサー（parser.rs）に統合
- [ ] `NksfFile` 構造体に `plugin_id: PlidData` フィールドを追加
- [ ] `unknown_chunks` から PLID を削除するロジックを実装
- [ ] ユニットテストを `plid_parser.rs` に追加
- [ ] 統合テストを `abandoned_test.rs` に追加
- [ ] `cargo test -p nksf-parser` が成功すること
- [ ] `cargo clippy` で警告が出ないこと

## 実装ガイド

### PlidData構造体（types.rs）

```rust
/// PLIDチャンク（Plugin ID）
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PlidData {
    /// VSTマジックナンバー
    #[serde(rename = "VST.magic")]
    pub vst_magic: u32,

    /// プラグイン名（オプション）
    #[serde(rename = "pluginName")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub plugin_name: Option<String>,

    /// プラグインベンダー名（オプション）
    #[serde(rename = "pluginVendor")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub plugin_vendor: Option<String>,
}
```

### PLIDパーサー（plid_parser.rs）

```rust
use crate::error::{ParseError, Result};
use crate::types::PlidData;

/// PLIDチャンクデータを解析
///
/// # Arguments
/// * `data` - チャンクデータ（バージョン情報を含む）
///
/// # Returns
/// * `Result<PlidData>` - 解析されたプラグインID
pub fn parse_plid_chunk(data: &[u8]) -> Result<PlidData> {
    if data.len() < 4 {
        return Err(ParseError::InvalidNiks);
    }

    let version = u32::from_le_bytes([data[0], data[1], data[2], data[3]]);

    if version != 1 {
        return Err(ParseError::InvalidNiks);
    }

    let plid_data: PlidData = rmp_serde::from_slice(&data[4..])?;

    Ok(plid_data)
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_parse_plid_full() {
        // 完全版PLID（73バイト）のテストデータ
        // VST.magic: 1315513416 (0x4E692448)
        // pluginName: "Massive X"
        // pluginVendor: "Native Instruments"
    }

    #[test]
    fn test_parse_plid_minimal() {
        // 簡易版PLID（20バイト）のテストデータ
        // VST.magicのみ
    }
}
```

### メインパーサー統合（parser.rs）

```rust
// parse_nksf_from_reader関数内で：

match chunk_id {
    "NISI" => {
        metadata = Some(parse_nisi_chunk(&data)?);
    }
    "NICA" => {
        parameters = Some(parse_nica_chunk(&data)?);
    }
    "PLID" => {
        plugin_id = Some(parse_plid_chunk(&data)?);
    }
    _ => {
        unknown_chunks.push(UnknownChunk { ... });
    }
}

// 必須チャンク確認
let plugin_id = plugin_id.ok_or(ParseError::InvalidNiks)?;
```

### NksfFile構造更新（types.rs）

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct NksfFile {
    pub metadata: NisiMetadata,
    pub parameters: NicaData,
    pub plugin_id: PlidData,  // 追加
    pub unknown_chunks: Vec<UnknownChunk>,
}
```

## テスト内容

### ユニットテスト（plid_parser.rs）

- 完全版PLIDの解析テスト
- 簡易版PLIDの解析テスト
- バージョンエラーテスト
- サイズ不足エラーテスト

### 統合テスト（abandoned_test.rs）

```rust
#[test]
fn test_abandoned_plid() {
    let path = get_fixture_path();
    let nksf = parse_nksf(&path).expect("Failed to parse");

    assert_eq!(nksf.plugin_id.vst_magic, 1315513416);
    assert_eq!(nksf.plugin_id.plugin_name, Some("Massive X".to_string()));
    assert_eq!(nksf.plugin_id.plugin_vendor, Some("Native Instruments".to_string()));
}

#[test]
fn test_abandoned_no_plid_in_unknown_chunks() {
    let path = get_fixture_path();
    let nksf = parse_nksf(&path).expect("Failed to parse");

    // PLIDはunknown_chunksに含まれないこと
    assert!(!nksf.unknown_chunks.iter().any(|c| c.id == "PLID"));
}
```

## 注意点

- pluginNameとpluginVendorはオプショナルフィールド
- 全プリセットでVST.magic = 1315513416を確認
- lib.rsのエクスポートに`PlidData`を追加
- `use crate::plid_parser::parse_plid_chunk;` をparser.rsに追加

---

## 実装メモ

[実装時に発見した事柄や改善点などを記載]
