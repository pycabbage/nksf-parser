# 009-004: PCHKパーサーの実装

## 概要

調査結果に基づき、PCHKチャンクパーサー（zlib展開 + MessagePackストリーム）を実装する。

## 完了条件

- [ ] flate2クレートを本番依存関係として追加
- [ ] `lib/src/types.rs` に `PchkData` 構造体を追加
- [ ] `lib/src/pchk_parser.rs` を作成
- [ ] PCHKパーサーをメインパーサー（parser.rs）に統合
- [ ] `NksfFile` 構造体に `plugin_chunk: PchkData` フィールドを追加
- [ ] `unknown_chunks` から PCHK を削除するロジックを実装
- [ ] ユニットテストを `pchk_parser.rs` に追加
- [ ] 統合テストを `abandoned_test.rs` に追加
- [ ] `cargo test -p nksf-parser` が成功すること
- [ ] `cargo clippy` で警告が出ないこと

## 実装ガイド

### 依存関係の追加

```bash
cargo add flate2 -p nksf-parser
```

### PchkData構造体（types.rs）

```rust
/// PCHKチャンク（Plugin Chunk）
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PchkData {
    /// ヘッダー情報
    pub header: PchkHeader,

    /// 全MessagePack値（268個）
    /// 構造: [name1, count1, data1, name2, count2, data2, ...]
    pub values: Vec<serde_json::Value>,
}

/// PCHKヘッダー（20バイト）
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PchkHeader {
    /// バージョン（通常1）
    pub version: u32,

    /// 不明フィールド1（通常2）
    pub field1: u32,

    /// 不明フィールド2（通常2）
    pub field2: u32,

    /// 圧縮データサイズ
    pub compressed_size: u32,

    /// 不明フィールド3
    pub field3: u32,
}
```

### PCHKパーサー（pchk_parser.rs）

```rust
use crate::error::{ParseError, Result};
use crate::types::{PchkData, PchkHeader};
use flate2::read::ZlibDecoder;
use std::io::Read;

/// PCHKチャンクデータを解析
///
/// # Arguments
/// * `data` - チャンクデータ（ヘッダー + zlib圧縮データ）
///
/// # Returns
/// * `Result<PchkData>` - 解析されたプラグインチャンク
pub fn parse_pchk_chunk(data: &[u8]) -> Result<PchkData> {
    if data.len() < 20 {
        return Err(ParseError::InvalidNiks);
    }

    // ヘッダー解析
    let header = PchkHeader {
        version: u32::from_le_bytes([data[0], data[1], data[2], data[3]]),
        field1: u32::from_le_bytes([data[4], data[5], data[6], data[7]]),
        field2: u32::from_le_bytes([data[8], data[9], data[10], data[11]]),
        compressed_size: u32::from_le_bytes([data[12], data[13], data[14], data[15]]),
        field3: u32::from_le_bytes([data[16], data[17], data[18], data[19]]),
    };

    if header.version != 1 {
        return Err(ParseError::InvalidNiks);
    }

    // zlib展開
    let compressed_data = &data[20..];
    let mut decoder = ZlibDecoder::new(compressed_data);
    let mut decompressed = Vec::new();
    decoder.read_to_end(&mut decompressed)
        .map_err(|e| ParseError::IoError(e))?;

    // MessagePackストリームから全値を読み取る
    let mut cursor = std::io::Cursor::new(&decompressed[..]);
    let mut values = Vec::new();

    loop {
        let pos = cursor.position();
        if pos >= decompressed.len() as u64 {
            break;
        }

        let value: serde_json::Value = rmp_serde::from_read(&mut cursor)
            .map_err(|e| ParseError::MessagePackError(e))?;

        values.push(value);

        // 安全装置
        if values.len() > 100000 {
            return Err(ParseError::InvalidNiks);
        }
    }

    // 全バイトが消費されたことを確認
    if cursor.position() != decompressed.len() as u64 {
        return Err(ParseError::IncompleteParse(
            (decompressed.len() as u64 - cursor.position()) as usize,
            cursor.position() as usize,
        ));
    }

    Ok(PchkData { header, values })
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_pchk_header_parsing() {
        // ヘッダーのみのテスト
    }

    #[test]
    fn test_pchk_too_short() {
        let data = [0x01, 0x02];
        let result = parse_pchk_chunk(&data);
        assert!(matches!(result, Err(ParseError::InvalidNiks)));
    }
}
```

### メインパーサー統合（parser.rs）

```rust
use crate::pchk_parser::parse_pchk_chunk;

// parse_nksf_from_reader内
match chunk_id {
    "NISI" => { metadata = Some(parse_nisi_chunk(&data)?); }
    "NICA" => { parameters = Some(parse_nica_chunk(&data)?); }
    "PLID" => { plugin_id = Some(parse_plid_chunk(&data)?); }
    "PCHK" => { plugin_chunk = Some(parse_pchk_chunk(&data)?); }
    _ => { unknown_chunks.push(UnknownChunk { ... }); }
}

// 必須チャンク確認
let plugin_id = plugin_id.ok_or(ParseError::InvalidNiks)?;
let plugin_chunk = plugin_chunk.ok_or(ParseError::InvalidNiks)?;
```

### lib.rs更新

```rust
mod pchk_parser;

pub use types::{
    // ...
    PlidData,
    PchkData,
    PchkHeader,
};
```

## テスト内容

### ユニットテスト

- ヘッダー解析テスト
- zlib展開テスト
- MessagePackストリーム解析テスト
- エラーケーステスト

### 統合テスト（abandoned_test.rs）

```rust
#[test]
fn test_abandoned_pchk_complete() {
    let path = get_fixture_path();
    let nksf = parse_nksf(&path).expect("Failed to parse");

    // ヘッダー検証
    assert_eq!(nksf.plugin_chunk.header.version, 1);
    assert_eq!(nksf.plugin_chunk.header.field1, 2);
    assert_eq!(nksf.plugin_chunk.header.field2, 2);
    assert_eq!(nksf.plugin_chunk.header.compressed_size, 31684);

    // 値の総数検証
    assert_eq!(nksf.plugin_chunk.values.len(), 268);

    // セクション名の検証
    let section_names: Vec<&str> = nksf.plugin_chunk.values.iter()
        .filter_map(|v| v.as_str())
        .filter(|s| !s.contains("/"))
        .collect();

    assert!(section_names.contains(&"strings"));
    assert!(section_names.contains(&"floats"));
    assert!(section_names.contains(&"doubles"));
    assert!(section_names.contains(&"ints"));
    assert!(section_names.contains(&"bools"));
    assert!(section_names.contains(&"charVecs"));
    assert!(section_names.contains(&"intVecs"));
    assert!(section_names.contains(&"floatVecs"));
    assert!(section_names.contains(&"doubleVecs"));
    assert!(section_names.contains(&"stringVecs"));
}

#[test]
fn test_abandoned_no_pchk_in_unknown_chunks() {
    let path = get_fixture_path();
    let nksf = parse_nksf(&path).expect("Failed to parse");

    // PCHKはunknown_chunksに含まれないこと
    assert!(!nksf.unknown_chunks.iter().any(|c| c.id == "PCHK"));
}
```

## 注意点

- flate2は本番依存関係として追加（dev依存ではない）
- 全268個のMessagePack値を保持
- 展開後のデータを全て保存（構造化済み）
- エラーハンドリングを適切に実装
- セクションの意味は解析済み（調査ドキュメント参照）

---

## 実装メモ

[実装時に発見した事柄や改善点などを記載]
